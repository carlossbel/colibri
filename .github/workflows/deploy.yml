name: Deploy to VPS

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Deploy Frontend to VPS
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.SERVER_IP }}
        username: ${{ secrets.SERVER_USER }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        script: |
          echo "🚀 Desplegando Colibri Frontend..."
          
          cd /home/deployer/colibri
          git pull origin main
          
          echo "🎨 Compilando Frontend..."
          cd colibri-frontend
          
          # Crear .env si no existe
          echo "VITE_API_URL=https://dashboard.render.com/d/dpg-d3ec4s15pdvs7390g5s0-a" > .env
          
          npm install
          npm run build
          
          echo "🔄 Reiniciando Nginx..."
          sudo systemctl reload nginx
          
          echo "✅ Frontend desplegado exitosamente: $(date)"
          echo "📦 Archivos en: /home/deployer/colibri/colibri-frontend/dist"
